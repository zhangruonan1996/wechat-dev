server:
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1 # 不限制请求体大小

spring:
  application:
    name: file-service
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 20MB

# 日志级别
logging:
  level:
    root: info
